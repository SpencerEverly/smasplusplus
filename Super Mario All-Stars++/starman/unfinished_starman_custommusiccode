function starman.customStarMusicLoad(p, characterID)

	customStarMusic = Misc.resolveSoundFile(customStarPath)

	p = p or player;
	characterID = characterID or p.character;
	local customStarMusicOgg = "starman.ogg"
	local customStarMusicSpc = "starman.spc"
	local customStarPath
	local costume = Player.getCostume(characterID)
	if costume then
		customStarPath = Misc.resolveFile("costumes\\"..chars[characterID].name.."\\"..costume.."\\".."starman.ogg") or Misc.resolveFile("costumes\\"..chars[characterID].name.."\\"..costume.."\\".."starman.spc")
	end
	
	if (iniFilePath == nil) then
	return;
	end
end

	starmanMusicChunk = Audio.SfxOpen(customStarMusic)
	if (customStarMusic == nil) then